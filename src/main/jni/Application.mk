#ANDROID APPLICATION MAKEFILE
#APP_PROJECT_PATH := $(call my-dir)
	
APP_OPTIM 		:= release
APP_PLATFORM    := android-7
APP_STL         := gnustl_static
APP_CPPFLAGS    := -frtti
APP_ABI         := armeabi armeabi-v7a x86
APP_MODULES     := Wabbitemu